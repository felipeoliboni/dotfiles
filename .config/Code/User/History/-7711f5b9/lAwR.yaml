apiVersion: v1 # Versão da API do Kubernetes
kind: Pod # Tipo do recurso
metadata: # Metadados do Pod
  name: meu-pod # Nome do Pod
  labels: # Lista de labels
    app: web # Label app com valor web
    version: blue # Label version com valor blue
spec: # Especificação do Pod
  containers: # Lista de containers
    - name: web # Nome do container
      image: kubedevio/web-color:blue # Imagem do container
      ports: # Lista de portas
        - name: http
          containerPort: 80 # Porta que o container expõe
          protocol: TCP # Protocolo da porta
---
apiVersion: v1 # Versão da API do Kubernetes
kind: Pod # Tipo do recurso
metadata: # Metadados do Pod
  name: meu-pod # Nome do Pod
  labels: # Lista de labels
    app: web # Label app com valor web
    version: green # Label version com valor green
spec: # Especificação do Pod
  containers: # Lista de containers
    - name: web # Nome do container
      image: kubedevio/web-color:green # Imagem do container
      ports: # Lista de portas
        - name: http
          containerPort: 80 # Porta que o container expõe
          protocol: TCP # Protocolo da porta
